name: Primer pipeline
on:
  pull_request:
    branches: [ "main" ]
  push: 
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      name: Analisis del proyecto
    - name: Ejecución de comandos
      run: echo Esta ejecutando el pipeline
    - name: Ejecucion de varios comandos
      run: |
        echo Varias lineas
        ls -la
